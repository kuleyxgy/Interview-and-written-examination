#include <stdio.h>

int main(void)
{
    (void)puts("sensor framework host demo: application modules available");
    (void)puts("integration assembly is supplied by the board profile");
    return 0;
}
